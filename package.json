{
  "name": "puppeteer-discord-bot",
  "version": "1.0.0",
  "description": "Discord bot with Puppeteer",
  "main": "los2.cjs",
  "scripts": {
    "setup-and-start": "npm run install-chrome && npm run start",
    "start": "node los2.cjs",
    "install-chrome": "puppeteer install --force",
    "debug": "node -e \"console.log(require('puppeteer').executablePath())\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "keywords": [],
  "author": "Reaea",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.16.3",
    "dotenv": "^10.0.0",
    "puppeteer": "^23.10.2"
  }
}
